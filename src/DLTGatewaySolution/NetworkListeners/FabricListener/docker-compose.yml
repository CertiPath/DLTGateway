version: '3.7'

services:
  dlt-fabric-listener:
    image: dlt-fabric-listener-img
    env_file:
      - node-variables.env
    secrets:
      - SQL_LOGIN_PWD
secrets:
  SQL_LOGIN_PWD:
    file: ./SQL_LOGIN_PWD.secret
    
