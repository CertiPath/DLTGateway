using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using CertiPath.BlockchainGateway.Model;
using Microsoft.AspNetCore.Mvc;


namespace CertiPath.BlockchainGateway.Web.Controllers
{
    [Route("api/[controller]")]
    public class BusinessNetworkController : Controller
    {
        /*
        [HttpGet("[action]")]
        public IEnumerable<BusinessNetworkModel> GetAll()
        {
            CertiPath.BlockchainGateway.Service.BusinessNetwork srvBN = new Service.BusinessNetwork();
            var list = srvBN.GetAll();
            return list;
        }
        */
    }
}
